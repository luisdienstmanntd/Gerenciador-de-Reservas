rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Exige login válido via Firebase Authentication para ler/escrever
    // qualquer documento. Publicado no console em 02/07/2026.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}